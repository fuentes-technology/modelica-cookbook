# Model authoring style

Shared by every `author-modelica-model*` skill. Match the style, units, and naming already used in the file or in sibling models in the project rather than inventing new conventions. Don't reinvent the wheel: browse the Modelica Standard Library for components and connectors that fit, and reuse them instead of writing custom equations for something MSL already models.
